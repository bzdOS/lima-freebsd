--- a/sys/dev/clk/allwinner/aw_clk_frac.c
+++ b/sys/dev/clk/allwinner/aw_clk_frac.c
@@ -120,6 +120,26 @@
 }
 
 static int
+aw_clk_frac_get_gate(struct clknode *clk, bool *enabled)
+{
+	struct aw_clk_frac_sc *sc;
+	uint32_t val;
+
+	sc = clknode_get_softc(clk);
+
+	if ((sc->flags & AW_CLK_HAS_GATE) == 0)
+		return (ENOENT);
+
+	DEVICE_LOCK(clk);
+	READ4(clk, sc->offset, &val);
+	DEVICE_UNLOCK(clk);
+
+	*enabled = (val & (1 << sc->gate_shift)) != 0;
+
+	return (0);
+}
+
+static int
 aw_clk_frac_set_mux(struct clknode *clk, int index)
 {
 	struct aw_clk_frac_sc *sc;
@@ -335,6 +355,7 @@
 	/* Device interface */
 	CLKNODEMETHOD(clknode_init,		aw_clk_frac_init),
 	CLKNODEMETHOD(clknode_set_gate,		aw_clk_frac_set_gate),
+	CLKNODEMETHOD(clknode_get_gate,		aw_clk_frac_get_gate),
 	CLKNODEMETHOD(clknode_set_mux,		aw_clk_frac_set_mux),
 	CLKNODEMETHOD(clknode_recalc_freq,	aw_clk_frac_recalc),
 	CLKNODEMETHOD(clknode_set_freq,		aw_clk_frac_set_freq),
--- a/sys/dev/clk/allwinner/aw_clk_m.c
+++ b/sys/dev/clk/allwinner/aw_clk_m.c
@@ -110,6 +110,26 @@
 }
 
 static int
+aw_clk_m_get_gate(struct clknode *clk, bool *enabled)
+{
+	struct aw_clk_m_sc *sc;
+	uint32_t val;
+
+	sc = clknode_get_softc(clk);
+
+	if ((sc->flags & AW_CLK_HAS_GATE) == 0)
+		return (ENOENT);
+
+	DEVICE_LOCK(clk);
+	READ4(clk, sc->offset, &val);
+	DEVICE_UNLOCK(clk);
+
+	*enabled = (val & (1 << sc->gate_shift)) != 0;
+
+	return (0);
+}
+
+static int
 aw_clk_m_set_mux(struct clknode *clk, int index)
 {
 	struct aw_clk_m_sc *sc;
@@ -242,6 +262,7 @@
 	/* Device interface */
 	CLKNODEMETHOD(clknode_init,		aw_clk_m_init),
 	CLKNODEMETHOD(clknode_set_gate,		aw_clk_m_set_gate),
+	CLKNODEMETHOD(clknode_get_gate,		aw_clk_m_get_gate),
 	CLKNODEMETHOD(clknode_set_mux,		aw_clk_m_set_mux),
 	CLKNODEMETHOD(clknode_recalc_freq,	aw_clk_m_recalc),
 	CLKNODEMETHOD(clknode_set_freq,		aw_clk_m_set_freq),
--- a/sys/dev/clk/allwinner/aw_clk_mipi.c
+++ b/sys/dev/clk/allwinner/aw_clk_mipi.c
@@ -109,6 +109,28 @@
 	return (0);
 }
 
+static int
+aw_clk_mipi_get_gate(struct clknode *clk, bool *enabled)
+{
+	struct aw_clk_mipi_sc *sc;
+	uint32_t val;
+
+	sc = clknode_get_softc(clk);
+
+	/*
+	 * Unlike the other aw_clk_* classes, aw_clk_mipi_set_gate() has no
+	 * AW_CLK_HAS_GATE guard -- PLL_MIPI always has a real gate (plus the
+	 * LDO1/LDO2 supply bits). Mirror that here: no flags check.
+	 */
+	DEVICE_LOCK(clk);
+	READ4(clk, sc->offset, &val);
+	DEVICE_UNLOCK(clk);
+
+	*enabled = (val & (1 << sc->gate_shift)) != 0;
+
+	return (0);
+}
+
 static uint64_t
 aw_clk_mipi_find_best(struct aw_clk_mipi_sc *sc, uint64_t fparent, uint64_t *fout,
     uint32_t *factor_k, uint32_t *factor_m, uint32_t *factor_n)
@@ -230,6 +252,7 @@
 	/* Device interface */
 	CLKNODEMETHOD(clknode_init,		aw_clk_mipi_init),
 	CLKNODEMETHOD(clknode_set_gate,		aw_clk_mipi_set_gate),
+	CLKNODEMETHOD(clknode_get_gate,		aw_clk_mipi_get_gate),
 	CLKNODEMETHOD(clknode_recalc_freq,	aw_clk_mipi_recalc),
 	CLKNODEMETHOD(clknode_set_freq,		aw_clk_mipi_set_freq),
 	CLKNODEMETHOD_END
--- a/sys/dev/clk/allwinner/aw_clk_nkmp.c
+++ b/sys/dev/clk/allwinner/aw_clk_nkmp.c
@@ -115,6 +115,26 @@
 }
 
 static int
+aw_clk_nkmp_get_gate(struct clknode *clk, bool *enabled)
+{
+	struct aw_clk_nkmp_sc *sc;
+	uint32_t val;
+
+	sc = clknode_get_softc(clk);
+
+	if ((sc->flags & AW_CLK_HAS_GATE) == 0)
+		return (ENOENT);
+
+	DEVICE_LOCK(clk);
+	READ4(clk, sc->offset, &val);
+	DEVICE_UNLOCK(clk);
+
+	*enabled = (val & (1 << sc->gate_shift)) != 0;
+
+	return (0);
+}
+
+static int
 aw_clk_nkmp_set_mux(struct clknode *clk, int index)
 {
 	struct aw_clk_nkmp_sc *sc;
@@ -345,6 +365,7 @@
 	/* Device interface */
 	CLKNODEMETHOD(clknode_init,		aw_clk_nkmp_init),
 	CLKNODEMETHOD(clknode_set_gate,		aw_clk_nkmp_set_gate),
+	CLKNODEMETHOD(clknode_get_gate,		aw_clk_nkmp_get_gate),
 	CLKNODEMETHOD(clknode_set_mux,		aw_clk_nkmp_set_mux),
 	CLKNODEMETHOD(clknode_recalc_freq,	aw_clk_nkmp_recalc),
 	CLKNODEMETHOD(clknode_set_freq,		aw_clk_nkmp_set_freq),
--- a/sys/dev/clk/allwinner/aw_clk_nm.c
+++ b/sys/dev/clk/allwinner/aw_clk_nm.c
@@ -111,6 +111,26 @@
 }
 
 static int
+aw_clk_nm_get_gate(struct clknode *clk, bool *enabled)
+{
+	struct aw_clk_nm_sc *sc;
+	uint32_t val;
+
+	sc = clknode_get_softc(clk);
+
+	if ((sc->flags & AW_CLK_HAS_GATE) == 0)
+		return (ENOENT);
+
+	DEVICE_LOCK(clk);
+	READ4(clk, sc->offset, &val);
+	DEVICE_UNLOCK(clk);
+
+	*enabled = (val & (1 << sc->gate_shift)) != 0;
+
+	return (0);
+}
+
+static int
 aw_clk_nm_set_mux(struct clknode *clk, int index)
 {
 	struct aw_clk_nm_sc *sc;
@@ -289,6 +309,7 @@
 	/* Device interface */
 	CLKNODEMETHOD(clknode_init,		aw_clk_nm_init),
 	CLKNODEMETHOD(clknode_set_gate,		aw_clk_nm_set_gate),
+	CLKNODEMETHOD(clknode_get_gate,		aw_clk_nm_get_gate),
 	CLKNODEMETHOD(clknode_set_mux,		aw_clk_nm_set_mux),
 	CLKNODEMETHOD(clknode_recalc_freq,	aw_clk_nm_recalc),
 	CLKNODEMETHOD(clknode_set_freq,		aw_clk_nm_set_freq),
--- a/sys/dev/clk/allwinner/aw_clk_nmm.c
+++ b/sys/dev/clk/allwinner/aw_clk_nmm.c
@@ -97,6 +97,26 @@
 	return (0);
 }
 
+static int
+aw_clk_nmm_get_gate(struct clknode *clk, bool *enabled)
+{
+	struct aw_clk_nmm_sc *sc;
+	uint32_t val;
+
+	sc = clknode_get_softc(clk);
+
+	if ((sc->flags & AW_CLK_HAS_GATE) == 0)
+		return (ENOENT);
+
+	DEVICE_LOCK(clk);
+	READ4(clk, sc->offset, &val);
+	DEVICE_UNLOCK(clk);
+
+	*enabled = (val & (1 << sc->gate_shift)) != 0;
+
+	return (0);
+}
+
 static uint64_t
 aw_clk_nmm_find_best(struct aw_clk_nmm_sc *sc, uint64_t fparent, uint64_t *fout,
   uint32_t *factor_n, uint32_t *factor_m0, uint32_t *factor_m1)
@@ -224,6 +244,7 @@
 	/* Device interface */
 	CLKNODEMETHOD(clknode_init,		aw_clk_nmm_init),
 	CLKNODEMETHOD(clknode_set_gate,		aw_clk_nmm_set_gate),
+	CLKNODEMETHOD(clknode_get_gate,		aw_clk_nmm_get_gate),
 	CLKNODEMETHOD(clknode_recalc_freq,	aw_clk_nmm_recalc),
 	CLKNODEMETHOD(clknode_set_freq,		aw_clk_nmm_set_freq),
 	CLKNODEMETHOD_END
--- a/sys/dev/clk/allwinner/aw_clk_np.c
+++ b/sys/dev/clk/allwinner/aw_clk_np.c
@@ -96,6 +96,26 @@
 	return (0);
 }
 
+static int
+aw_clk_np_get_gate(struct clknode *clk, bool *enabled)
+{
+	struct aw_clk_np_sc *sc;
+	uint32_t val;
+
+	sc = clknode_get_softc(clk);
+
+	if ((sc->flags & AW_CLK_HAS_GATE) == 0)
+		return (ENOENT);
+
+	DEVICE_LOCK(clk);
+	READ4(clk, sc->offset, &val);
+	DEVICE_UNLOCK(clk);
+
+	*enabled = (val & (1 << sc->gate_shift)) != 0;
+
+	return (0);
+}
+
 static uint64_t
 aw_clk_np_find_best(struct aw_clk_np_sc *sc, uint64_t fparent, uint64_t *fout,
     uint32_t *factor_n, uint32_t *factor_p)
@@ -212,6 +232,7 @@
 	/* Device interface */
 	CLKNODEMETHOD(clknode_init,		aw_clk_np_init),
 	CLKNODEMETHOD(clknode_set_gate,		aw_clk_np_set_gate),
+	CLKNODEMETHOD(clknode_get_gate,		aw_clk_np_get_gate),
 	CLKNODEMETHOD(clknode_recalc_freq,	aw_clk_np_recalc),
 	CLKNODEMETHOD(clknode_set_freq,		aw_clk_np_set_freq),
 	CLKNODEMETHOD_END
